import { NextFunction, Request, Response } from 'express';

export const requestValidator = (
	req: Request,
	response: Response,
	next: NextFunction
) => {
	// TODO: Add logic to validate requests
};
